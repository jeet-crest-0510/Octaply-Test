import type { AiPromptResponse } from '../api';
export type ProcessScreenshotsResponse = {
    index: number;
    success: boolean;
    binaryData?: Buffer;
    mimeType?: string;
    error?: Error;
};
export declare function processScreenshots(response: AiPromptResponse): Promise<ProcessScreenshotsResponse[]>;
